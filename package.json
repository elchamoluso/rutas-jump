{
  "name": "rutas-jump-ui",
  "private": true,
  "version": "1.0.0",
  "description": "Build-time only. React Spectrum UI for the rutas-jump dashboard. The committed ui/dashboard.bundle.js is what ships; the runtime (generate-dashboard.js) stays node-only and just inlines that bundle.",
  "scripts": {
    "build": "node build.js",
    "build:watch": "node build.js --watch"
  },
  "dependencies": {
    "@adobe/react-spectrum": "^3.41.0",
    "@spectrum-icons/workflow": "^4.2.20",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
